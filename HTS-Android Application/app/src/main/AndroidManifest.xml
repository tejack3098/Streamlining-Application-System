<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="c.gpp.scanner">

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon2"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon2"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".Notification.NotificationActivity"></activity>
        <activity android:name=".ArrivingFiles.ArrivingFilesActivity" />
        <activity android:name=".PreviousFiles.PreviousFilesActivity" />
        <activity android:name=".EmpStats.EmpStatsActivity" />
        <activity android:name=".CurrentFiles.CurFilesActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Home.MainScanActivity" />
        <activity android:name=".Scan.ScanCodeActivity" />
        <activity android:name=".Login.MainActivity" />
    </application>



</manifest>